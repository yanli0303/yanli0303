export interface Profile {
  label: string;
  username: string;
  urlTemplate: string;
  iconName: string;
  iconColor: string;
  textColor: string;
  leftBackgroundColor: string;
  rightBackgroundColor: string;
}
